export OVPN_SERVER_CN="${OVPN_SERVER_CN:-}"
export GENERATE_CLIENT_CERTIFICATE="${GENERATE_CLIENT_CERTIFICATE:-true}"
export OVPN_DIR="${OVPN_DIR:-/etc/openvpn}"
export OVPN_INTERFACE_NAME="${OVPN_INTERFACE_NAME:-tun}"
export CONFIGFILES="${CONFIGFILES:-/opt/configuration}"
export EASYRSA_DIR="${EASYRSA_DIR:-/opt/easyrsa}"
export PKI_DIR="${PKI_DIR:-${OVPN_DIR}/pki}"
export LOG_DIR="${LOG_DIR:-${OVPN_DIR}/logs}"
export OVPN_TLS_CIPHERS="${OVPN_TLS_CIPHERS:-TLS-ECDHE-ECDSA-WITH-CHACHA20-POLY1305-SHA256:TLS-ECDHE-RSA-WITH-CHACHA20-POLY1305-SHA256:TLS-ECDHE-ECDSA-WITH-AES-128-GCM-SHA256:TLS-ECDHE-RSA-WITH-AES-128-GCM-SHA256}"
export OVPN_TLS_CIPHERSUITES="${OVPN_TLS_CIPHERSUITES:-TLS-AES-256-GCM-SHA384:TLS-CHACHA20-POLY1305-SHA256:TLS-AES-128-GCM-SHA256:TLS-AES-128-CCM-8-SHA256:TLS-AES-128-CCM-SHA256}"
export OVPN_PORT="${OVPN_PORT:-1194}"
export OVPN_PORT_CLIENT_SIDE="${OVPN_PORT_CLIENT_SIDE:-1194}"
export OVPN_PROTOCOL="${OVPN_PROTOCOL:-tcp}"
export OVPN_INTERFACE_NAME="${OVPN_INTERFACE_NAME:-}"
export OVPN_NETWORK="${OVPN_NETWORK:-10.50.50.0 255.255.255.0}"
export OVPN_VERBOSITY="${OVPN_VERBOSITY:-3}"
export OVPN_NAT="${OVPN_NAT:-true}"
export OVPN_REGISTER_DNS="${OVPN_REGISTER_DNS:-false}"
export OVPN_DNS_SERVERS="${OVPN_DNS_SERVERS:-}"
export REGENERATE_CERTS="${REGENERATE_CERTS:-false}"
export OVPN_MANAGEMENT_ENABLE="${OVPN_MANAGEMENT_ENABLE:-false}"
export OVPN_MANAGEMENT_NO_AUTH="${OVPN_MANAGEMENT_NO_AUTH:-false}"
export OVPN_MANAGEMENT_PORT="${OVPN_MANAGEMENT_PORT:-5000}"
export DEBUG="${DEBUG:-false}"
export LOG_FILE="${LOG_FILE:-${LOG_DIR}/openvpn.log}"
export KEY_LENGTH="${KEY_LENGTH:-2048}"
export LDAP_FILE="${OVPN_DIR}/auth/ldap.conf"
export OVPN_LDAP_HOST="${OVPN_LDAP_HOST:-}"
export OVPN_LDAP_BindDN="${OVPN_LDAP_BindDN:-}"
export OVPN_LDAP_BindPassword="${OVPN_LDAP_BindPassword:-}"
export OVPN_LDAP_BaseDN="${OVPN_LDAP_BaseDN:-}"
export OVPN_LDAP_USERATTRIBUTE="${OVPN_LDAP_USERATTRIBUTE:-sAMAccountName}"
export OVPN_LDAP_GROUPMEMBER="${OVPN_LDAP_GROUPMEMBER:-}"
export OVPN_LDAP_CONF="${OVPN_LDAP_CONF:-}"